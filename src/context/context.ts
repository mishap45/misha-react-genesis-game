import React from "react";

const StepContext = React.createContext({});
const StepContextSet = React.createContext({});
const CashContext = React.createContext({});
const CashContextSet = React.createContext({});

export { StepContext, StepContextSet, CashContext, CashContextSet };